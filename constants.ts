import { TaskStatus, Priority, Category, Project, Task } from './types';

// Empty initial state - new users start with a blank workspace
export const INITIAL_PROJECTS: Project[] = [];

export const INITIAL_TASKS: Task[] = [];